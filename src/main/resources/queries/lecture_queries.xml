<?xml version="1.0" encoding="UTF-8" ?>

<properties>
    <query key="lecture.findAllBySectionIds">
        SELECT
        lecture_id,
        section_id,
        name,
        lecture_time,
        order_num,
        video_url,
        created_at,
        updated_at
        FROM Lecture
        WHERE section_id IN (:in_clause)
        ORDER BY order_num
    </query>
</properties>
